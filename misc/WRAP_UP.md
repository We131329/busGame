# Wrap Up Checklist

- [x] **Game Progression**: Implement `getGameProgression()` in `Game.php`.
- [x] **Zombie Mode**: Implement the `zombie()` method in each state class (under `modules/php/States/`).
- [x] **Statistics**: Define and implement meaningful statistics in `stats.jsonc`.
- [x] **Game Logs**: Ensure logs explain what happened clearly for players who weren't looking.
- [x] **Tiebreaking**: Implement tiebreaking using the auxiliary score field and update the description in metadata.
- [x] **Translations**: Mark all UI strings for translation using `_()` in JS and `clienttranslate()` in PHP.
- [x] **Tooltips**: Add tooltips to images/UI elements like cards and tokens for better UX.
